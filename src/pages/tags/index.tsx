import { Divider } from "@mui/material";
import React from "react";

function tags() {
  return (
    <div>
      tags
      <Divider sx={{ borderBottomWidth: 5, borderColor: "red" }} />
    </div>
  );
}

export default tags;
